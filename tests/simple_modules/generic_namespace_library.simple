module Tests.GenericNamespaceLibrary

Box<T> :: artifact {
  value : T
}

GenericValues :: namespace {
  identity<T> :: (value : T) -> T {
    return value
  }

  stored : Box<i32> = GenericValues.identity<Box<i32>>({ 42 })
}
