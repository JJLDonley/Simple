import System.Channel

main : () -> i32 {
  ch : i64 = System.Channel.newString()
  if (!System.Channel.sendString(ch, "hello")) { return 0 }
  s : string = System.Channel.recvString(ch)
  System.Channel.close(ch)
  return len(s)
}
