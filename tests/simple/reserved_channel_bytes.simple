import System.Channel

main : () -> i32 {
  ch : i64 = System.Channel.newBytes()
  data : i32[] = [1, 2, 3, 4]
  if (!System.Channel.sendBytes(ch, data)) { return 0 }
  out : i32[] = System.Channel.recvBytes(ch)
  System.Channel.close(ch)
  return len(out) + out[0] + out[3]
}
