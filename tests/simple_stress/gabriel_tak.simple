module Tests.Stress.GabrielTak

tak :: (x : i32, y : i32, z : i32) -> i32 {
  if (y >= x) { return z }
  return tak(
    tak(x - 1, y, z),
    tak(y - 1, z, x),
    tak(z - 1, x, y))
}

main :: () -> i32 {
  if (tak(14, 8, 4) != 5) { return 1 }
  return 0
}
