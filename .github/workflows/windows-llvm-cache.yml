name: Windows LLVM Cache

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - cmake/platform/vcpkg/windows-llvm/**
      - cmake/platform/triplets/x64-windows-static-release.cmake
      - .github/workflows/windows-llvm-cache.yml

permissions:
  contents: read

jobs:
  seed-vcpkg-cache:
    runs-on: windows-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Restore LLVM binary cache
        id: cache
        uses: actions/cache/restore@v6
        with:
          path: ${{ runner.temp }}/vcpkg-bincache
          key: windows-vcpkg-llvm-${{ hashFiles('cmake/platform/vcpkg/windows-llvm/**', 'cmake/platform/triplets/x64-windows-static-release.cmake') }}

      - name: Build LLVM 18 binary package
        if: steps.cache.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          $vcpkgRoot = Join-Path $env:RUNNER_TEMP "vcpkg"
          $binaryCache = Join-Path $env:RUNNER_TEMP "vcpkg-bincache"
          git clone https://github.com/microsoft/vcpkg.git $vcpkgRoot
          git -C $vcpkgRoot checkout f4a2b74e2c6e4e419a3d26c41b5fd6e64a711de9
          & "$vcpkgRoot/bootstrap-vcpkg.bat"
          $env:VCPKG_ROOT = $vcpkgRoot
          $env:VCPKG_FORCE_SYSTEM_BINARIES = "1"
          $env:VCPKG_DEFAULT_BINARY_CACHE = $binaryCache
          $env:VCPKG_OVERLAY_TRIPLETS = Join-Path $env:GITHUB_WORKSPACE "cmake/platform/triplets"
          New-Item -ItemType Directory -Force -Path $binaryCache | Out-Null
          $manifestRoot = Join-Path $env:GITHUB_WORKSPACE "cmake/platform/vcpkg/windows-llvm"
          $installRoot = Join-Path $vcpkgRoot "installed"
          & "$vcpkgRoot/vcpkg.exe" install `
            --x-manifest-root=$manifestRoot `
            --x-install-root=$installRoot `
            --triplet=x64-windows-static-release
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Save LLVM binary cache
        if: steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@v6
        with:
          path: ${{ runner.temp }}/vcpkg-bincache
          key: windows-vcpkg-llvm-${{ hashFiles('cmake/platform/vcpkg/windows-llvm/**', 'cmake/platform/triplets/x64-windows-static-release.cmake') }}
