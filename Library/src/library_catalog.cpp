#include "library_catalog.h"

// Library catalog definitions are currently header-only while call sites migrate
// to the shared Library/ boundary. Keep this translation unit so build targets
// own the shared catalog component explicitly.
