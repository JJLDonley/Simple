import Channel

main : i32 () {
  ch : i64 = Channel.newI32()
  if (!Channel.sendI32(ch, 42)) { return 0 }
  a : i32 = Channel.tryRecvI32(ch)
  Channel.sendI32(ch, 7)
  b : i32 = Channel.recvI32(ch)
  Channel.close(ch)
  return a + b
}
