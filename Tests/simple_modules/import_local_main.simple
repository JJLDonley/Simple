import "./import_local_lib"
import "import_local_sub"
import Standard.IO

main : i32 () {
  if (local_add(40, 2) != 42) { return 1 }
  if (sub_value() != 7) { return 2 }
  Standard.IO.println("imports work!")
  return 0
}
